# Local development defaults only. Do not put hosted credentials in this file.
PIA_ENVIRONMENT=development

# Server-only local development URL. apps/web must never read this value.
PIA_DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:54322/postgres
