# Safe, browser-visible example configuration only.
NEXT_PUBLIC_APP_NAME=PIA
# Public browser values only. Obtain the local anon key from `supabase status`.
NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Local API origin for the authenticated import-review flow.
NEXT_PUBLIC_PIA_API_URL=http://localhost:8000
