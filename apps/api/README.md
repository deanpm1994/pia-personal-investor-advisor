# API workspace

This directory is reserved for the typed FastAPI service in P1.3. P1.1 establishes
only the Python and uv workspace boundary; it deliberately has no service, database,
authentication, provider, or financial-domain code.
