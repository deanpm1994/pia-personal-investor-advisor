# Security policy

PIA is a personal-finance project. Do not report vulnerabilities through public issues when they could expose credentials, authentication behavior, user data, import files, or provider configuration.

Use a private channel with the repository owner and include:

- affected component;
- reproduction steps using synthetic data only;
- impact assessment;
- suggested mitigation if available.

Do not attach broker exports, access tokens, session tokens, or real screenshots.
